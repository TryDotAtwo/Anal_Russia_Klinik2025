# config

Ручные настройки проекта.

- `manual_filters.csv` - рабочий CSV для правил `keep/reject/review`.
- Переменные окружения имеют префикс `ANAL_RUSSIA_KLINIK_`.
- Старый префикс `BOT_ANAL_` поддержан как compatibility fallback.
